# Зависимости веб-версии. Расчётное ядро плюс тонкий веб-слой.
# PySide6 здесь нет намеренно: интерфейс настольной программы серверу не нужен,
# а весит он больше всего остального вместе взятого.

# Ядро расчёта.
#
# numpy<2 — не осторожность, а требование площадки: виртуальный процессор
# сервера (QEMU 2.5+) не поддерживает набор инструкций x86-64-v2, с которым
# собраны колёса NumPy 2.x, и импорт падает при старте. Сборки 1.26
# рассчитаны на SSE3 и работают.
skyfield>=1.49
numpy>=1.24,<2
pandas>=2.0
pyarrow>=14.0
requests>=2.31
sgp4>=2.23
matplotlib>=3.8

# Веб
fastapi>=0.110
uvicorn[standard]>=0.27
jinja2>=3.1
python-multipart>=0.0.9
itsdangerous>=2.1
